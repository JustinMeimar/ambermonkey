name = "7-8-aot-attachment-cost"
description = "Randomized blocked cpstartup measurement of AOT image attachment, metadata reconstruction, residual compilation, and end-to-end content-process startup"
default_iterations = 10
allow_failure = false
workdir = "."

[analyze]
startup-cost = "analyses/parse_startup_cost.py"

[figures.cost-tables]
analysis = "startup-cost"
script = "figures/cost_tables.py"

[variables]
MOZCONFIG = "/home/justin/spidermonkey/ambermonkey/mozconfigs/browser-release-aot.mozconfig"

[variants]
blocked = "python3 scripts/run_block.py --firefox @FIREFOX --binary @BIN_AOT --mozconfig $MOZCONFIG"
