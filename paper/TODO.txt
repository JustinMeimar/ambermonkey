


- [ ] The Runtime Indirection Overhead measures cycles per-iteratoin. The
      point we are trying to make is nuanced: These 8 micro-benchmarks are
      designed to exercise the areas of AmberMonkey where our mechanism is the
      most expesnive.

      What we might consider including is a vertical partition - on the LHS the
      graph remains exactly as is, on the RHS we have a second axis which is raw
      performance on Speedometer3 and JetStream three comparing two configurations:

      - 1) stock --no-ion v.s 2) ambermonkey --no-ion --aot. This restricts both
        executino tiers to Baseline JIT compilation at maximum and incurs the
        full area of the AOT image overhead on the system.

- [ ] Create master list of execution configurations. Apply ubitquitously to
      all labels, figures, and prose in the paper.
